# 功能域2：订单与车辆 — 完整开发说明

**负责人**: hws、zj  
**开发分支**: `hws-ride-dev`  
**完成日期**: 2026-04-14  
**最近更新**: 2026-04-23  
**文档面向**: 全体组员，无需了解本域代码也能读懂

---

## 目录

1. [这个域做了什么](#一这个域做了什么)
2. [用户能看到什么界面](#二用户能看到什么界面)
3. [后端：10个接口详解](#三后端10个接口详解)
4. [前端：页面与路由清单](#四前端页面与路由清单)
5. [订单状态流转图](#五订单状态流转图)
6. [数据结构](#六数据结构)
7. [如何在本地启动](#七如何在本地启动)
8. [与其他功能域的对接方式](#八与其他功能域的对接方式)
9. [对原框架的修改说明](#九对原框架的修改说明)
10. [测试账号与种子数据](#十测试账号与种子数据)
11. [常见问题 FAQ](#十一常见问题-faq)
12. [2026-04-23 更新说明](#十二2026-04-23-更新说明)

---

## 一、这个域做了什么

功能域2负责整个拼车系统的**订单核心**，包括：

- 拼车人发布订单（选路线、时间、座位需求、标签偏好）
- 其他用户搜索/浏览可拼订单
- 车主选择自己的车辆接单，锁定行程
- 行程结束后标记完成
- 管理员可强制取消任何订单
- 车主名下车辆信息的查询

简单说：**从订单发布到订单完成的全生命周期**，都在这个域里。

> **当前状态**：功能已初步完成，可运行全流程测试。Mock 模式下无需数据库，切换真实 MySQL 需额外配置（见第七节）。

---

## 二、用户能看到什么界面

### 拼车人视角

| 页面 | 访问路径 | 能做什么 |
|------|----------|---------|
| 乘客首页 | `/passenger/home` | 快速进入搜索、查看自己的订单、看到近期记录 |
| 发布订单 | `/passenger/orders/publish` | 填写出发地/目的地（有输入建议）、出发时间窗口、同行人数、期望车费、选择标签 |
| 搜索订单 | `/passenger/orders/search` | 按路线/时间/标签筛选招募中的订单，点击卡片查看详情 |
| 我的订单 | `/passenger/orders/mine` | 查看自己发布的全部订单，按状态 tab 切换 |
| 订单详情 | `/passenger/orders/:id` | 查看完整信息、参与拼单、取消订单 |

**发布订单页功能亮点：**
- 输入出发地/目的地时会弹出预置地点建议（可输入关键词过滤）
- `我方人数`（含本人）+ `还能带几人` 合并计算"共占座位"
- 实时显示"每人均价"（= 期望总价 ÷ 总人数）
- 标签分为预置标签（点击选中/取消）和自定义标签（输入后回车/点 + 添加）

### 车主视角

| 页面 | 访问路径 | 能做什么 |
|------|----------|---------|
| 车主首页 | `/driver/home` | 看到待接订单数、进行中订单数，快速跳转 |
| 可接订单 | `/driver/orders/available` | 浏览所有招募中的订单，点"接单"弹出车辆选择 |
| 我的接单 | `/driver/orders/mine` | 查看自己已接的订单，locked 状态可标记完成或取消 |

**接单流程：**
1. 在"可接订单"列表里找到合适的订单
2. 点"接单 →"，弹出底部弹窗
3. 从自己名下车辆列表选择一辆（自动拉取，只有一辆时自动选中）
4. 点"确认接单"，订单状态变为 `locked`

### 管理员视角

| 页面 | 访问路径 | 能做什么 |
|------|----------|---------|
| 订单管理 | `/admin/orders` | 查看全部订单，按关键词搜索，按状态筛选，强制取消任意未完成订单 |

---

## 三、后端：10个接口详解

服务地址（本地）：通过网关 `http://localhost:8000` 访问，实际由 8002 端口的订单域处理。

### 接口鉴权约定

所有接口通过 HTTP Header 传递用户身份（无 JWT，符合课设约定）：

```
X-User-Id: dev-user-1        # 当前用户的 userId
X-User-Role: admin           # 仅管理员操作时附加
```

---

### 接口1：发布订单

```
POST /api/orders
```

**请求示例**：
```json
{
  "start_loc": "软件园",
  "end_loc": "大学城",
  "depart_time_from": "2026-04-15T08:00:00",
  "depart_time_to": "2026-04-15T09:00:00",
  "group_size": 2,
  "extra_seats": 1,
  "expected_price": 45.0,
  "tags": ["静音", "禁烟"]
}
```

字段说明：
- `group_size`：本人一行几人（含本人），最少为 1
- `extra_seats`：还能再带几个陌生人上车，0 表示不拼其他人
- 系统计算 `seats_needed = group_size + extra_seats`（总座位需求）

**成功响应**：
```json
{ "order_id": "ord-xxxxxxxx", "status": "published" }
```

---

### 接口2：搜索订单

```
GET /api/orders/search?start_loc=南门&tags=静音
```

**查询参数**（全部可选，不传则不限制该条件）：

| 参数 | 说明 | 示例 |
|------|------|------|
| `start_loc` | 出发地关键词（模糊匹配） | `南` 可匹配"南门"、"南区" |
| `end_loc` | 目的地关键词（模糊匹配） | |
| `time_from` | 出发时间下限 | `2026-04-15T08:00:00` |
| `time_to` | 出发时间上限 | |
| `tags` | 标签筛选，逗号分隔，**需全部命中** | `静音,禁烟` |

> 只返回状态为 `published` 或 `full` 的订单（招募中）

**响应**：返回订单列表，每条订单包含 `remaining_seats`（剩余可拼座位数）

---

### 接口3：订单列表

```
GET /api/orders?passenger_id=dev-user-1
GET /api/orders?owner_id=dev-user-1
GET /api/orders                          ← 管理员用，返回全部
```

---

### 接口4：订单详情

```
GET /api/orders/{order_id}
```

返回单条订单的完整信息，包括 `locked_time`（车主接单时间）、`vehicle_id` 等。

**错误码**：`404`（订单不存在）

---

### 接口5：修改订单

```
PUT /api/orders/{order_id}
```

**限制**：只有发单人本人，且订单处于 `published` 状态时才能修改。

**错误码**：`403`（非本人）、`409`（状态不允许修改）

---

### 接口6：取消订单

```
POST /api/orders/{order_id}/cancel
```

**谁可以取消**：
- 发单人（拼车人）本人
- 已接单的车主
- 已参与拼单的其他乘客
- **管理员**（附加 `X-User-Role: admin` header）

> 如果订单处于 `locked` 状态时取消，响应中 `penalty: true`，表示需要扣除信誉积分（积分扣除由功能域3处理，本域只标记结果）

**错误码**：`403`（无权限）、`409`（订单已结束）

---

### 接口7：乘客拼单（加入订单）

```
POST /api/orders/{order_id}/join
```

**场景**：发单人发的订单有空余座位，其他拼车人想一起拼，点"参与拼单"。

> 座位满时订单状态自动从 `published` 变为 `full`

**错误码**：`409`（已参与 / 已满员 / 状态不允许）

---

### 接口8：车主接单

```
POST /api/orders/{order_id}/accept
Body: { "vehicle_id": "veh-001" }
```

车主选择用哪辆车接单，订单状态变为 `locked`，同时记录 `owner_id`、`vehicle_id`、`locked_time`。

**错误码**：`409`（已被接单 / 状态不允许）

---

### 接口9：标记完成

```
POST /api/orders/{order_id}/complete
Body: { "operator": "driver" }    ← 车主手动完成
Body: { "operator": "domain3" }   ← 支付完成后由功能域3回调
```

订单状态从 `locked` 变为 `completed`。

**错误码**：`409`（订单未处于 locked 状态）

---

### 接口10：查询车辆列表

```
GET /api/vehicles
Header: X-User-Id: dev-user-1
```

返回该车主名下全部车辆，用于接单时选车。

---

## 四、前端：页面与路由清单

### 新增文件

`frontend/src/api/ride.js` — 所有 API 调用统一封装，其他组员如需调用订单/车辆相关接口，引入这个文件即可：

```javascript
import { rideApi } from '@/api/ride.js'

const res = await rideApi.searchOrders({ start_loc: '南门' })
await rideApi.publishOrder({ ... })
```

此外还导出以下常量：
- `STATUS_MAP`：订单状态到中文标签/颜色的映射
- `AVAILABLE_TAGS`：预置标签列表（静音、禁烟、宠物友好…）
- `LOCATION_SUGGESTIONS`：预置地点建议列表
- `calcPerPersonPrice(price, group, extra)`：计算每人均价

### 完整路由与文件对应

```
/login                         → views/common/LoginPage.vue

/passenger/home                → views/passenger/PassengerHome.vue
/passenger/orders/publish      → views/passenger/PassengerOrderPublish.vue
/passenger/orders/search       → views/passenger/PassengerOrderSearch.vue
/passenger/orders/mine         → views/passenger/PassengerOrderMine.vue
/passenger/orders/:id          → views/passenger/PassengerOrderDetail.vue

/driver/home                   → views/owner/OwnerHome.vue
/driver/orders/available       → views/owner/OwnerOrderAvailable.vue
/driver/orders/mine            → views/owner/OwnerOrderMine.vue

/admin/orders                  → views/admin/AdminOrders.vue
```

所有页面都包在 `layouts/AppLayout.vue` 里（底部导航栏）。

---

## 五、订单状态流转图

```
                  ┌─────────────────────────────────────────┐
                  │                                         │
         发布订单 ▼                                         │
           ┌──────────┐   座位满员      ┌──────────┐       │
           │published │ ─────────────▶ │  full    │       │
           │ (招募中) │ ◀────────────── │ (已满员) │       │
           └────┬─────┘   有人退出      └──────────┘       │
                │                                         │
                │ 车主接单（选车辆）                         │
                ▼                                         │
           ┌──────────┐   标记完成    ┌───────────┐       │
           │  locked  │ ────────────▶ │ completed │       │
           │ (已锁单) │              │ (已完成)  │       │
           └──────────┘              └───────────┘       │
                │                                         │
                │ 取消（发单人/车主/乘客/管理员）            │
                ▼                                         │
           ┌──────────┐                                   │
           │cancelled │ ◀─────────────────────────────────┘
           │ (已取消) │   （published/full/locked 均可取消）
           └──────────┘
```

| 状态 | 中文 | 颜色标识 | 说明 |
|------|------|----------|------|
| `published` | 招募中 | 蓝色 | 可被搜索、可接单、可拼单 |
| `full` | 已满员 | 黄色 | 座位满了，不再接受新乘客 |
| `locked` | 已锁单 | 橙色 | 车主已接单，等待出发 |
| `completed` | 已完成 | 绿色 | 行程结束 |
| `cancelled` | 已取消 | 灰色 | 已取消，信誉积分可能扣除 |

---

## 六、数据结构

### 订单对象（`orders` 表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `order_id` | string | 唯一ID，格式 `ord-xxxxxxxx` |
| `passenger_id` | string | 发单人的 userId |
| `start_loc` | string | 出发地 |
| `end_loc` | string | 目的地 |
| `depart_time_from` | ISO8601 | 最早出发时间 |
| `depart_time_to` | ISO8601 | 最晚出发时间 |
| `group_size` | int | 发单人同行人数 |
| `extra_seats` | int | 额外可带人数 |
| `seats_needed` | int | 总座位需求 = group_size + extra_seats |
| `seats_joined` | int | 当前已加入人数 |
| `remaining_seats` | int | 剩余座位（查询时动态计算） |
| `expected_price` | float | 期望总车费 |
| `owner_id` | string\|null | 车主 userId（接单后填入） |
| `vehicle_id` | string\|null | 车辆ID（接单后填入） |
| `locked_time` | ISO8601\|null | 锁单时间 |
| `status` | enum | 见上方状态表 |
| `tags` | string[] | 标签列表 |
| `created_at` | ISO8601 | 创建时间 |

### 车辆对象（`vehicles` 表）

| 字段 | 类型 | 说明 |
|------|------|------|
| `vehicle_id` | string | 唯一ID |
| `owner_id` | string | 车主 userId |
| `plate_no` | string | 车牌号，如"粤B·12345" |
| `brand` | string | 品牌型号，如"本田 雅阁" |
| `color` | string | 颜色 |
| `seat_capacity` | int | 最大载客数 |
| `status` | enum | `available` / `unavailable` |

---

## 七、如何在本地启动

### 环境要求

- Python 3.9+
- Node.js 18+（`node -v` 确认）

### 第一步：安装依赖

```bash
# Python 依赖（在 Software-project/ 目录）
pip install fastapi uvicorn python-dotenv

# 前端依赖（在 frontend/ 目录）
npm install
```

### 第二步：启动后端

打开**两个**终端，都在 `Software-project/` 目录下：

```bash
# 终端1：网关
python -m uvicorn backend.gateway.main:app --port 8000 --reload

# 终端2：订单域
python -m uvicorn backend.ride.service:app --port 8002 --reload
```

### 第三步：启动前端

```bash
cd frontend
npm run dev
# 访问 http://localhost:5173
```

### 第四步：登录

目前前端有 dev 模式快速切换：在登录页可以直接切换 `dev-user-1`（乘客）、`dev-user-2`（车主）、`admin`（管理员）角色，无需真实登录流程。

---

## 八、与其他功能域的对接方式

### 对功能域1（账号域 · wyx）

**本域需要账号域提供的信息：**

用户登录成功后，账号域需要将以下信息写入 `localStorage`：

```javascript
localStorage.setItem('session', JSON.stringify({
  userId: 'user-001',   // 用户唯一ID（字符串）
  role: 'passenger',    // 'passenger' | 'driver' | 'admin'
  username: 'alice'     // 显示名称（可选）
}))
```

本域读取 `userId` 作为 `X-User-Id` header，读取 `role` 决定显示哪个 Tab（乘客/车主/管理员）。

> ⚠️ `role` 字段必须是以上三个值之一，否则底部导航栏无法正确显示。如果账号域用的字段名不同，告知 hws 调整映射。

**本域不依赖账号域的接口**，不会调用 `/api/auth/*`。

---

### 对功能域3（交易域 · yzr）

**功能域3可以调用本域的接口：**

1. **订单完成回调**：支付成功后，域3调用以下接口将订单标记为完成：
   ```
   POST /api/orders/{order_id}/complete
   Header: X-User-Id: {domain3-service-id}
   Body: { "operator": "domain3" }
   ```

2. **查询订单金额**：域3计算拆单金额时，通过订单详情接口获取 `expected_price` 和 `seats_needed`：
   ```
   GET /api/orders/{order_id}
   ```
   每人应付 = `expected_price ÷ seats_needed`（前端用相同逻辑显示每人均价）

**本域不调用功能域3的接口**，不处理支付流程。

---

## 九、对原框架的修改说明

本节说明相对于初始框架（`init` commit）做了哪些修改，以及原因。

### 后端

| 文件 | 原框架状态 | 本次改动 |
|------|-----------|---------|
| `backend/ride/service.py` | 仅有 4 个骨架接口（search无参数、publish无校验、accept/confirm接口路径与约定不符） | 重写为 10 个完整接口，统一路径规范，加入参数校验和 Header 鉴权 |
| `backend/ride/ride_domain.py` | 1 条种子数据，字段用驼峰命名（`orderId`、`ownerId`），无状态机逻辑 | 重写为完整业务逻辑，字段改为下划线命名（与前端 API 契约对齐），加入状态机、标签筛选、Mock/DB 双模式 |
| `backend/common/config.py` | 读取 `ACCOUNT_USE_MOCK` 等变量 | 新增 `RIDE_USE_MOCK` 变量读取 |
| `backend/gateway/main.py` | 转发规则指向端口 8002 | 补全转发逻辑，端口保持 8002 不变 |
| `sql/ride/001_init.sql` | 原字段为驼峰，部分字段缺失 | 更新为与 `ride_domain.py` 保持一致的下划线字段 |

> 端口沿用原框架约定（网关 8000、账号域 8001、订单域 8002、运营域 8003），未做修改。

### 前端

| 文件 | 原框架状态 | 本次改动 |
|------|-----------|---------|
| `frontend/src/api/ride.js` | **不存在** | 新建，统一封装全部接口调用 |
| 全部 `views/` 页面 | 占位页面（仅有标题文字，无实际功能） | 按需求逐一实现 |
| `layouts/AppLayout.vue` | `session` 使用 `computed(() => getSession())`，切换角色后 Tab 栏不刷新 | 改为 `ref(getSession())`，切换时显式更新，修复了该响应式缺陷 |
| `frontend/vite.config.js` | 代理指向 8000 端口 | 保持不变 |

---

## 十、测试账号与种子数据

> **2026-04-23 起**：测试数据改为由 `sql/ride/002_seed.sql` 管理，默认走真实数据库（`RIDE_USE_MOCK=false`）。Mock 模式仍可通过 `.env` 设置 `RIDE_USE_MOCK=true` 启用，但种子数据以 SQL 为准。

### 测试账号

| userId | 角色 | 说明 |
|--------|------|------|
| `dev-user-1` | 乘客 | 发布了 ord-seed-001、ord-seed-003 |
| `dev-user-2` | 乘客 | 发布了 ord-seed-002 |
| `dev-user-3` | 乘客 | 发布了 ord-seed-004 |
| `dev-owner-1` | 车主 | 接了 ord-seed-003，名下有一辆车（粤A·88888） |

### 种子订单

| order_id | 路线 | 状态 | 说明 |
|----------|------|------|------|
| `ord-seed-001` | 软件园 → 大学城 | `published` | 含标签"静音""禁烟"，可测试搜索和接单 |
| `ord-seed-002` | 天河客运站 → 广州南站 | `published` | 含标签"宠物友好" |
| `ord-seed-003` | 珠江新城 → 广州白云机场 | `locked` | dev-owner-1 已接，可测试标记完成 |
| `ord-seed-004` | 南门 → 广州南站 | `published` | 含标签"准时出发""禁烟" |

### 种子车辆

| vehicle_id | 车主 | 车牌 | 车型 |
|-----------|------|------|------|
| 1 | dev-owner-1 | 粤A·88888 | 丰田 凯美瑞 / 珍珠白 / 5座 |
| 2 | dev-user-1  | 粤B·12345 | 本田 雅阁 / 深空黑 / 5座 |
| 3 | dev-user-1  | 粤C·67890 | 大众 帕萨特 / 银色 / 5座 |

### 推荐测试流程

```
1. 以 dev-user-1（乘客）登录
   → 发布订单（软件园 → 大学城，明天 08:00-09:00，2 人）
   → 搜索"软件园"，确认能找到刚发布的订单

2. 切换为 dev-owner-1（车主）
   → 进"可接订单"，找到上面发布的订单
   → 点接单 → 选车辆（粤A·88888）→ 确认接单
   → 进"我的接单"，确认状态变为 locked

3. 在"我的接单"点"✓ 标记完成"
   → 确认状态变为 completed

4. 切换为 admin（管理员）
   → 进"订单管理"，对 published 订单点"强制取消"
   → 确认状态变为 cancelled
```

---

## 十一、常见问题 FAQ

**Q: 启动时提示端口被占用怎么办？**

先查是谁占了：
```bash
netstat -ano | findstr :8002
```
如果是之前的 Python 进程没退出，Ctrl+C 关掉或任务管理器结束 Python 进程即可。

---

**Q: 切换角色后页面乱了怎么办？**

点"退出登录"彻底清空 session 后重新选角色，或者在 Chrome 开发者工具 → Application → Local Storage 里删除 `session` 键。

---

**Q: Mock 模式下数据会消失？**

是的，Mock 用内存存储，重启服务后重置。**2026-04-23 起默认已切换为真实数据库模式**（`RIDE_USE_MOCK=false`），需要本地有 MySQL 并执行初始化脚本（见第十二节）。如需临时使用 Mock，在 `.env` 里设置 `RIDE_USE_MOCK=true` 即可。

---

**Q: 功能域3怎么知道一笔订单完成了？**

两种方案：
- **推荐**：支付成功后，域3主动调用 `POST /api/orders/{id}/complete`，body 传 `{"operator": "domain3"}`
- 备选：域3轮询 `GET /api/orders/{id}` 查询状态

---

## 十二、2026-04-23 更新说明

本次更新源于组长审核 PR 后的反馈：要求测试数据改用 SQL 脚本管理，并验证真实数据库路径。

### 主要变更

#### 测试数据改为 SQL 管理

新增 `sql/ride/002_seed.sql`，将原来写在 `ride_domain.py` 里的 Mock 内存数据迁移为 SQL 种子脚本。Mock 模式保留作为可选项，但默认关闭。

`backend/common/config.py` 中 `RIDE_USE_MOCK` 默认值由 `True` 改为 `False`。

#### SQL 初始化脚本说明

本域共两个 SQL 文件，按顺序执行：

**`sql/ride/001_init.sql` — 建库建表**

创建 `ride_db` 数据库，建立以下 5 张表：

| 表名 | 说明 |
|------|------|
| `orders` | 订单主表，存储订单全生命周期数据 |
| `order_tag` | 订单标签表，一条订单可关联多个标签 |
| `order_passenger` | 参与乘客表，记录每位拼单乘客及支付状态 |
| `vehicle` | 车辆表，由 zj 负责维护，含认证状态字段 `verified` |
| `order_status_log` | 状态流转日志表，记录每次状态变更的操作人和时间 |

**`sql/ride/002_seed.sql` — 导入测试数据**

插入与 Mock 模式等价的种子数据：4 条订单、7 个标签、5 条参与记录、3 辆车辆、2 条状态日志。详见第十节。

> 两个文件开头均有 `SET NAMES utf8mb4`，确保中文字段在任何 MySQL 客户端下导入都不会乱码。

#### 本地数据库初始化步骤

```bash
# 在 MySQL 中按顺序执行（以 Docker 为例）
docker exec -i <容器名> mysql -u root -p<密码> < sql/ride/001_init.sql
docker exec -i <容器名> mysql -u root -p<密码> < sql/ride/002_seed.sql
```

`.env` 中配置数据库连接：
```
RIDE_DB_HOST=127.0.0.1
RIDE_DB_PORT=3306
RIDE_DB_NAME=ride_db
RIDE_DB_USER=root
RIDE_DB_PASSWORD=<你的密码>
RIDE_USE_MOCK=false
```

#### 其他修复

| 类别 | 修复内容 |
|------|---------|
| SQL Schema | 补回原框架遗漏的 `order_status_log` 表和 `vehicle.verified` 字段 |
| SQL Schema | `vehicle.owner_user_id` 类型由 `BIGINT` 改为 `VARCHAR(64)`，与系统字符串 userId 对齐 |
| 数据库连接 | `ride_db.py` 连接后显式执行 `SET NAMES utf8mb4`，修复中文乱码 |
| 前端布局 | 手机框高度固定（PC 端 844px），内容区改为内部滚动，页面不再撑高手机框 |
| 前端布局 | `van-tabbar` 加 `:fixed="false"`，弹层加 `transform: translateZ(0)`，所有浮层收归手机框内 |
| 前端布局 | 移除 `van-tabs` 的 `sticky` 属性（内容在 div 内滚动，window 不滚动，sticky 无效） |
